export default class Usuario {
    constructor(id_usuario,uid,mail,password,rol){
          this.id_usuario =  id_usuario,
          this.uid = uid,
          this.mail = mail,
          this.password = password,
          this.rol = rol
    }
}
